export { 
  createConnection, 
  getAvailableConnectionPresets, 
  getConnectionPreset
} from './factory';
export { connectionPresets } from './connectionPresets';
export type { ConnectionPreset, ConnectionPresetCategory } from './types';
